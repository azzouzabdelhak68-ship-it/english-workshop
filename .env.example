# ── Supabase ─────────────────────────────────────────────────────
# Project Settings → API in your Supabase dashboard
VITE_SUPABASE_URL=https://YOUR_PROJECT.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# ── Edge Function secrets ────────────────────────────────────────
# The Gemini key is NOT a static secret: staff rotate it at runtime via the
# AI Chat Room settings card (stored in Supabase Vault by update-ai-key).
# Film discovery uses Gemini google_search grounding directly — no YouTube or
# OMDb API keys are needed anymore (architecture changed 2026-08-23).
